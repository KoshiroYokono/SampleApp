$("favorite-buttons").html("<%= escape_javascript(render "favorites/unfollow", topic: @topic) %>");

// $("#favorite-buttons").html("<%= escape_javascript(render('favorites/unfollow'), locals:{topic: topic}) %>");
// topicsってどこにあるの？？
